run backend :
PS D:\online delivery>  uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000 [copy this and paste it on terminal]

run frontend: npm start for Expo QR in android application

DATABASE_URL = "mysql+pymysql://root:password@localhost:3306/online_delivery"

If testing on a real phone replace with PC's LAN IP e.g. http://192.168.1.50:8000
export const API_BASE = 'http://10.54.10.246:8005';
 replace the service/api.js API url with your PCs IP or LAN

run this command python -m alembic -c backend/alembic.ini revision --autogenerate -m "initial commit" for database alembic initialization
next step : python -m alembic -c backend/alembic.ini upgrade head    run this command 

for notifivation updtes 
in backend 
install dependencies :
"pip install passlib[bcrypt]"

"pip install python-jose"
"pip install fastapi"
"pip install python-multipart"
pip install "pydantic[email]" 
pip install "uvicorn[standard]"
pip isntall pymysql
pip install SQLAlchemy

for initial "python -m alembic -c backend/alembic.ini stamp base"

if  adding the new column in db make migration using 
"python -m alembic -c backend/alembic.ini revision --autogenerate -m "added contact_number to store"
and then "python -m alembic -c backend/alembic.ini upgrade head"

for geo current location fetching 
"expo install expo-location"

"npx expo install expo-location"